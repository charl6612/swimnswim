<%= simple_form_for [pool, rental] do |f| %>
  <%= f.input :date %>
  <%= f.input :comment %>
  <%= f.input :user_number %>
  <%= f.submit "Demande de réservation", class: 'btn btn-primary'  %>
<% end %>
