<%= link_to users_dashboard_pool_path(pool) do %>
  <div class="card-product">
    <%= cl_image_tag pool.picture %>
    <div class="card-product-infos">
      <h2><%= pool.name %></h2>
      <p><%=pool.address%></p>
      <p><%=pool.price_per_day%> € / jour</p>
      <p><%=pool.capacity%> personnes max</p>
    </div>
  </div>
<% end %>






